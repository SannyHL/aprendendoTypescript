/*

function somarNumeros(numer1, numer2) {
    if (typeof numer1 === "number" && typeof numer2 === "number") {
        return numer1 + numer2;
    }else{
        return Number(numer1) + Number(numer2);
    }
    
}

button.addEventListener('click', () => (
    console.log(somarNumeros(input1.value, input2.value))
))*/